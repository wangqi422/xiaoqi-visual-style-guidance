---
name: xiaoqi-visual-style-guidance
description: Use when users provide one or more visual references and need objective visual-style extraction, strict parseable JSON, reference-role separation, style transfer, composition locking, production-ready prompts for Nano Banana or GPT Image 2, or a controlled batch of at least ten GPT Image 2 variants after explicitly saying 开始生图. Trigger for requests mentioning 小柒视觉风格牵引, 风格提取, 风格迁移, 反推提示词, 图1/图2职责, 参考优先级, 构图不变, Nano Banana, nanobanana, GPT Image 2, gptimage2, or 开始生图.
---

# 小柒视觉风格牵引提示词

把参考图当作彼此隔离的视觉控制信号。先提取可观察的视觉特征，再为每张图分配唯一职责，最后按用户需要输出严格 JSON、平台提示词，或在获得明确触发后执行至少 10 张 GPT Image 2 受控变体生成。

## 核心规则

1. 只描述图中可见的视觉证据，不猜测作者、创作年代、品牌归属或不可见信息。
2. 将客观观察和专业推断分开；无法可靠判断时写 `unknown`，画面不存在文字时将排版状态写为 `not_applicable`。
3. 一张参考图只控制用户授权的维度。用户指定的职责和优先级始终覆盖默认判断。
4. 多图冲突时让高优先级参考覆盖低优先级参考，不平均融合，不使用“融合所有图片风格”作为职责说明。
5. 用户只要求分析、提取或优化提示词时，不调用生图工具。批量工作流只有在用户明确说出“开始生图”后才进入第二阶段。
6. 用户没有提供目标主体、场景或迁移任务时，只完成风格分析，将两个提示词的 `prompt` 保持为空字符串，不擅自创作内容。
7. “可以生图”“之后生图”“准备生图”等表达只说明意向，不构成第二阶段授权。

## 工作流程

### 1. 建立输入与职责表

按上传顺序把图片标记为 `image_1`、`image_2` 等。逐张判断其被授权负责的内容：

- 角色身份：面容、发型、体型、服装、配饰和标志性外观。
- 动作：身体方向、重心、肩胯关系、头部朝向、四肢路径和运动趋势。
- 构图：摄像机位置、机位高度、俯仰、焦距感、透视、裁切、主体占比、遮挡和景深层次。
- 武器或道具：轮廓、结构、尺寸比例、材质、握持或接触关系。
- 场景：建筑、地形、道具、天气、空间尺度和环境叙事。
- 风格：色彩、排版、光影、材质、后期、颗粒、渲染语言和整体氛围。

为每张图记录 `image_id`、`responsibilities`、`priority` 与 `do_not_inherit`。未获授权的内容写入 `do_not_inherit`。

### 2. 决定优先级

用户未指定时，使用以下默认顺序：

1. 构图与镜头
2. 动作与身体朝向
3. 人物、武器或道具的接触关系及比例
4. 角色身份与服装
5. 武器或道具造型
6. 场景
7. 色彩、光影、材质和后期风格

只在缺失信息会显著改变结果时询问用户；其余情况使用合理默认值并降低对应置信度。

### 3. 提取视觉风格

完整分析以下六个维度：

- 色彩：主色、辅助色、色温、饱和度和对比度。
- 排版：字体风格、字重、行距、层级和图文关系。
- 构图：布局、主体位置、视觉平衡、留白比例和前中后景。
- 光影：布光类型、主光方向、阴影性质、高光位置和动态范围。
- 质感与材质：表面纹理、具体材质、粗糙度、反射和渲染特征。
- 整体氛围：情绪基调、风格标签和商业视觉定位。

使用具体、可核验的描述，例如“低饱和青灰色背景配少量高亮橙色点缀”，不用“高级、震撼、氛围感强”等空泛形容词。

### 4. 选择输出模式

#### 严格 JSON

用户要求 JSON、结构化分析、自动化处理或同时要求两个平台提示词时，读取 [references/schema-and-platforms.md](references/schema-and-platforms.md)，严格按其中 Schema 输出一个 JSON 对象。

- 顶层字段固定为 `style_analysis`、`reference_roles`、`nanobanana_prompt`、`gpt_image_2_prompt` 和 `confidence`。
- 不添加 Markdown 代码围栏、注释、标题或 JSON 前后的解释。
- 保留全部顶层字段和必填子字段。
- 所有置信度使用 `0.0` 至 `1.0` 的数字。
- 用户未给出迁移目标时，把平台提示词写为空字符串。

#### 单平台提示词

用户只要求 Nano Banana 或 GPT Image 2 提示词时，先用一句中文说明参考图职责，再输出对应平台的一段完整、可直接复制的提示词和必要禁止项。读取 [references/schema-and-platforms.md](references/schema-and-platforms.md) 中对应平台章节。

#### 仅分析

用户只说“分析、提取风格、反推视觉特征”且没有指定 JSON 时，输出六维风格分析、参考职责、冲突和置信度，不生成虚构的迁移目标。

#### 两阶段 GPT Image 2 工作流

第一阶段始终先完成提示词优化：

1. 建立参考图职责与优先级。
2. 生成 GPT Image 2 主提示词，明确保持项、变更项与禁止继承项。
3. 检查构图、动作、身体方向、武器或道具比例、接触关系、透视、光源与材质。
4. 输出最终主提示词，不自动继续生成图片。

只有用户明确说“开始生图”时才执行第二阶段。进入第二阶段前必须同时具备：

- 当前对话中可访问的全部相关参考图。
- 已确认或刚完成优化的 GPT Image 2 最终主提示词。

缺少任一项时先补齐，不能用虚构图片、占位符或未经确认的旧提示词代替。满足条件后读取 [references/batch-generation.md](references/batch-generation.md)，默认生成 10 张成功图片；用户指定更多数量时按指定数量执行。

#### 其他直接生图

用户明确要求单张生成、创建或修改图片，但没有说“开始生图”时：

1. 先在内部完成职责拆分和平台提示词。
2. 将所有目标参考图传给图像工具。
3. 在工具提示词中保留图号、职责、优先级、保持项和禁止继承项。
4. 首轮结果违反最高优先级约束时，只强化失败约束，不随意改变已经正确的部分。

### 5. 锁定构图与动作

用户要求构图不变时必须明确：

- 严格锁定原图摄像机、机位高度、俯仰角、焦距感和透视关系。
- 不改变主体位置、身体朝向、主体占比、画面裁切、遮挡和前中后景关系。
- 不重新设计构图，不新增无关元素，不删除指定元素。

涉及人物、武器或道具时，继续明确身体方向、重心、双手接触点、物体尺寸比例、枪口或道具方向，以及是否遮挡面部。

## 输出前检查

- 图号与上传顺序一致，且每张图只承担被授权的职责。
- JSON 可直接解析，字段齐全，没有多余文本。
- 排版不存在时使用 `not_applicable`，不确定信息使用 `unknown`。
- 构图、动作、身体方向、比例和接触关系没有被风格参考覆盖。
- 光源方向、色温、材质反射与环境一致。
- 未无依据增加或删除人物、文字、Logo、水印、武器、道具或背景元素。
- Nano Banana 与 GPT Image 2 提示词分别适配，没有把同一段提示词直接互译复用。
